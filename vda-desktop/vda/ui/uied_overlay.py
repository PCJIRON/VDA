"""Shim for backward compatibility.

The original implementation lived in a single large file.  It has been split
into the ``vda.ui.uied_overlay`` package.  This file re‑exports the
public classes so existing import statements (e.g. ``from ...ui.uied_overlay
import UIEDOverlayWidget``) remain valid.
"""

