---
phase: 01-foundation-security
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - qwen-desktop/qwen_desktop/ui/assistant/__init__.py (new)
  - qwen-desktop/qwen_desktop/ui/assistant/controller.py (new -- FloatingAssistant, reduced)
  - qwen-desktop/qwen_desktop/ui/assistant/worker.py (new -- APIServerWorker)
  - qwen-desktop/qwen_desktop/ui/assistant/message_bubble.py (new -- MessageBubble)
  - qwen-desktop/qwen_desktop/ui/assistant/chat_popup.py (new -- ChatHistoryPopup)
  - qwen-desktop/qwen_desktop/ui/__init__.py (new or update)
  - qwen-desktop/qwen_desktop/ui/floating_assistant.py (delete or strip to re-export)
  - qwen-desktop/qwen_desktop/app.py (modify -- update import path)
  - qwen-desktop/tests/test_clicker_engine.py (new)
  - qwen-desktop/tests/test_safety.py (new)
  - qwen-desktop/tests/test_keychain.py (new)
autonomous: true
requirements:
  - RFCT-02
  - RFCT-05
  - TEST-02
user_setup: []

must_haves:
  truths:
    - "FloatingAssistant class exists in ui/assistant/controller.py and is importable"
    - "APIServerWorker, MessageBubble, ChatHistoryPopup each have their own file in ui/assistant/"
    - "app.py imports FloatingAssistant from qwen_desktop.ui.assistant.controller"
    - "Click execution logic in controller.py uses ClickerEngine instead of inline pyautogui calls"
    - "ui/floating_assistant.py either deleted or reduced to a minimal re-export"
    - "New test files exist for ClickerEngine, safety module, and keychain integration"
    - "pytest tests/ -x --tb=short -q passes with all existing + new test files"
    - "No Python file in qwen_desktop/ exceeds 100 lines except those justified per D-03"
  artifacts:
    - path: qwen-desktop/qwen_desktop/ui/assistant/controller.py
      provides: "FloatingAssistant main controller (~1500->~900 lines reduced)"
      exports: ["FloatingAssistant"]
    - path: qwen-desktop/qwen_desktop/ui/assistant/worker.py
      provides: "APIServerWorker (QThread for API streaming)"
      exports: ["APIServerWorker"]
    - path: qwen-desktop/qwen_desktop/ui/assistant/message_bubble.py
      provides: "MessageBubble chat widget"
      exports: ["MessageBubble"]
    - path: qwen-desktop/qwen_desktop/ui/assistant/chat_popup.py
      provides: "ChatHistoryPopup session history overlay"
      exports: ["ChatHistoryPopup"]
    - path: qwen-desktop/tests/test_clicker_engine.py
      provides: "Tests for ClickerEngine coordinate handling and strategy fallback"
      contains: "test_clicker_engine_pass_through"
    - path: qwen-desktop/tests/test_safety.py
      provides: "Tests for restore_failsafe context manager"
      contains: "test_restore_failsafe_saves_and_restores"
    - path: qwen-desktop/tests/test_keychain.py
      provides: "Tests for keyring-backed API key storage"
      contains: "test_get_api_key_fallback"
  key_links:
    - from: qwen-desktop/qwen_desktop/app.py
      to: qwen-desktop/qwen_desktop/ui/assistant/controller.py
      via: "from qwen_desktop.ui.assistant.controller import FloatingAssistant"
      pattern: "ui.assistant.controller"
    - from: qwen-desktop/qwen_desktop/ui/assistant/controller.py
      to: qwen-desktop/qwen_desktop/core/clickers/engine.py
      via: "from qwen_desktop.core.clickers import ClickerEngine"
      pattern: "ClickerEngine"

---

<objective>
Split the 1,988-line floating_assistant.py into a focused `ui/assistant/` sub-package and write tests for all refactored modules.

**Purpose:** Eliminate the god class pattern by separating concerns into single-responsibility files. Move click execution logic to the ClickerEngine. Add test coverage for new modules (ClickerEngine, safety, keychain) to satisfy TEST-02.

**Output:**
- `qwen_desktop/ui/assistant/` sub-package with controller.py, worker.py, message_bubble.py, chat_popup.py
- FloatingAssistant reduced from ~1,500 lines to focused controller (~900 lines) with click logic moved to ClickerEngine
- Existing `ui/floating_assistant.py` deleted (one-pass import update)
- Updated `app.py` import path
- 3 new test files: test_clicker_engine.py, test_safety.py, test_keychain.py
- Working test suite (pytest passes)
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-RESEARCH.md (floating_assistant split details)
@.planning/codebase/CONCERNS.md (god class, high coupling details)
@.planning/codebase/CONVENTIONS.md

Reference source files:
@qwen-desktop/qwen_desktop/ui/floating_assistant.py (full -- 1988 lines, all 4 classes)
@qwen-desktop/qwen_desktop/app.py (imports FloatingAssistant)
@qwen-desktop/qwen_desktop/utils/safety.py (from Plan 01-01)
@qwen-desktop/qwen_desktop/core/clickers/engine.py (from Plan 01-03)
@qwen-desktop/qwen_desktop/core/pyautogui_executor.py (referenced by FloatingAssistant)
@qwen-desktop/qwen_desktop/core/enhanced_executor.py (referenced by FloatingAssistant)
@qwen-desktop/tests/test_attachments.py (existing test pattern)
@qwen-desktop/tests/test_error_handler.py (existing test pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split floating_assistant.py into ui/assistant/ sub-package</name>
  <files>
    qwen-desktop/qwen_desktop/ui/assistant/__init__.py (new)
    qwen-desktop/qwen_desktop/ui/assistant/worker.py (new -- APIServerWorker)
    qwen-desktop/qwen_desktop/ui/assistant/message_bubble.py (new -- MessageBubble)
    qwen-desktop/qwen_desktop/ui/assistant/chat_popup.py (new -- ChatHistoryPopup)
    qwen-desktop/qwen_desktop/ui/assistant/controller.py (new -- FloatingAssistant, reduced)
    qwen-desktop/qwen_desktop/ui/__init__.py (new)
    qwen-desktop/qwen_desktop/ui/floating_assistant.py (delete or reduce to re-export shim)
    qwen-desktop/qwen_desktop/app.py (modify import path)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/ui/floating_assistant.py (full 1988 lines -- all classes, imports, methods)
    @qwen-desktop/qwen_desktop/app.py (line 36 -- the import to update)
    @qwen-desktop/qwen_desktop/core/clickers/engine.py (ClickerEngine API -- from Plan 01-03)
    @qwen-desktop/qwen_desktop/core/pyautogui_executor.py (referenced by FloatingAssistant)
    @qwen-desktop/qwen_desktop/core/enhanced_executor.py (referenced by FloatingAssistant)
    @qwen-desktop/qwen_desktop/utils/safety.py (restore_failsafe)
  </read_first>
  <acceptance_criteria>
    - Four classes live in four separate files under ui/assistant/
    - FloatingAssistant is reduced in scope (click execution logic moved to ClickerEngine or kept minimal)
    - app.py imports from qwen_desktop.ui.assistant.controller
    - python -c "from qwen_desktop.ui.assistant.controller import FloatingAssistant" succeeds
    - python -c "from qwen_desktop.ui.assistant.worker import APIServerWorker" succeeds
    - python -c "from qwen_desktop.ui.assistant.message_bubble import MessageBubble" succeeds
    - python -c "from qwen_desktop.ui.assistant.chat_popup import ChatHistoryPopup" succeeds
    - No import errors when running python -c "import qwen_desktop"
  </acceptance_criteria>
  <action>
    1. Create `qwen_desktop/ui/__init__.py`:
       - Barrel export if needed: `from qwen_desktop.ui.assistant.controller import FloatingAssistant` (optional -- app.py will use the full path per D-02 no-backward-compat rule).

    2. Create `qwen_desktop/ui/assistant/__init__.py`:
       - Barrel export all 4 classes: `from qwen_desktop.ui.assistant.worker import APIServerWorker`, `from qwen_desktop.ui.assistant.message_bubble import MessageBubble`, `from qwen_desktop.ui.assistant.chat_popup import ChatHistoryPopup`, `from qwen_desktop.ui.assistant.controller import FloatingAssistant`.
       - `__all__` listing all four.

    3. Create `qwen_desktop/ui/assistant/worker.py`:
       - Extract APIServerWorker class (currently lines 54-102 of floating_assistant.py).
       - Keep: QThread subclass, pyqtSignal declarations, __init__ with api_client/message/history/vision_mode, run(), _stream() methods.
       - Remove any import of FloatingAssistant or ChatHistoryPopup (the worker shouldn't depend on those).
       - Keep imports: `asyncio`, `logging`, `QThread`, `pyqtSignal`, `extract_thinking`.
       - Add module docstring: `"""QThread worker for streaming API responses."""`.

    4. Create `qwen_desktop/ui/assistant/message_bubble.py`:
       - Extract MessageBubble class (currently lines 105-269 of floating_assistant.py).
       - Keep: QWidget subclass, __init__ with text/sender/vision_mode/attachments, frame styling, thinking toggle, copy button, update_text(), set_thinking(), _toggle_thinking(), _copy_text().
       - Keep imports: `QWidget`, `QVBoxLayout`, `QHBoxLayout`, `QLabel`, `QPushButton`, `QFrame`, `QTimer`, `QApplication`, `Qt`, `QPixmap`, `datetime`, `logging`.
       - Add module docstring: `"""Chat message bubble widget -- user and AI message rendering with thinking toggle."""`.

    5. Create `qwen_desktop/ui/assistant/chat_popup.py`:
       - Extract ChatHistoryPopup class (currently lines 272-473 of floating_assistant.py).
       - Keep: QWidget subclass, frameless window styling, session list, message scroll area, staging area, populate_sessions(), clear_chat(), add_message(), update_last_message(), update_thinking(), scroll_to_bottom(), set_vision_status().
       - Keep imports: `QWidget`, `QVBoxLayout`, `QHBoxLayout`, `QLabel`, `QPushButton`, `QScrollArea`, `QFrame`, `QTimer`, `QGraphicsDropShadowEffect`, `QColor`, `Qt`, `logging`.
       - Add module docstring: `"""Chat history popup -- session list, message display, staging area for attachments."""`.

    6. Create `qwen_desktop/ui/assistant/controller.py`:
       - Extract FloatingAssistant class (currently lines 475-end of floating_assistant.py).
       - This is the large file. Key changes:
         - Replace inline click execution with ClickerEngine usage where appropriate.
         - The `_execute_uied_action` method should use `with restore_failsafe():` (already done in Plan 01-01).
         - The `_execute_vision_action` method calls `self._pyautogui_executor.execute()` -- keep this reference for now (PyAutoGUIExecutor is the active executor, not the dead clickers).
         - Import ClickerEngine from `qwen_desktop.core.clickers` for future use, but don't change the active click path yet (that would be behavioral change beyond refactoring).
         - Remove the `pyautogui.FAILSAFE = False/True` pattern (already handled by Plan 01-01).
         - Keep all other existing behavior unchanged.
       - Keep imports: all the QWidget imports from Qt, `logging`, `re`, `cv2`, `numpy`, `SessionService`, `VisionCaptureService`, `PyAutoGUIExecutor`, `EnhancedExecutor`, `ThinkingFilter`, `AutoTemplateExtractor`, `build_system_prompt`, `ProviderConfig`, `APIClient`, `ZenClient`, `ShortTermMemory`, `LongTermMemory`, `DailyTaskCache`, `TaskDecomposer`, `BehaviorTracker`, `uuid`, `uiautomation` (with try/except guard), all ui component imports, `UIEDOverlayWidget`, `SettingsDialog`, `ClickerEngine`.
       - Add import: `from qwen_desktop.utils.safety import restore_failsafe` (already added in Plan 01-01).
       - Add import: `from qwen_desktop.core.clickers import ClickerEngine` (for future wiring).
       - Keep class structure: all __init__ logic, event handlers, vision methods, UIED methods, send_message, signal handlers.
       - Module docstring: `"""Floating assistant controller -- main application window with chat, vision, and UIED functionality."""`.
       - Note: File will be ~900 lines (vs ~1500 originally) after extracting the 3 sub-classes.

    7. Update `qwen_desktop/app.py`:
       - Change line 36: `from qwen_desktop.ui.floating_assistant import FloatingAssistant` -> `from qwen_desktop.ui.assistant.controller import FloatingAssistant`.

    8. Delete `qwen_desktop/ui/floating_assistant.py`:
       - After verifying all classes have been extracted and app.py import updated, delete the original file.
       - Per D-02: no backward-compat re-exports. Delete the file, do NOT create a re-export shim.

    9. Verify: `python -c "import qwen_desktop; print('Package imports OK')"` succeeds.
  </action>
  <verify>
    <automated>Set-Location qwen-desktop; python -c "from qwen_desktop.ui.assistant.controller import FloatingAssistant; from qwen_desktop.ui.assistant.worker import APIServerWorker; from qwen_desktop.ui.assistant.message_bubble import MessageBubble; from qwen_desktop.ui.assistant.chat_popup import ChatHistoryPopup; print('All 4 assistant classes importable')"</automated>
  </verify>
  <done>
    Four classes extracted into separate files under ui/assistant/. app.py imports from the new path. Original floating_assistant.py deleted. Package imports cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests for ClickerEngine, safety, and keychain modules</name>
  <files>
    qwen-desktop/tests/test_clicker_engine.py (new)
    qwen-desktop/tests/test_safety.py (new)
    qwen-desktop/tests/test_keychain.py (new)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/core/clickers/engine.py (ClickerEngine, ClickStrategy, ClickResult)
    @qwen-desktop/qwen_desktop/utils/safety.py (restore_failsafe)
    @qwen-desktop/qwen_desktop/auth/provider_config.py (keychain integration)
    @qwen-desktop/tests/test_attachments.py (existing test pattern/structure)
    @qwen-desktop/tests/test_error_handler.py (existing test pattern/structure)
  </read_first>
  <acceptance_criteria>
    - test_clicker_engine.py has 3+ test functions covering: passthrough with no strategies, strategy fallback order, ClickResult structure
    - test_safety.py has 3+ test functions covering: FAILSAFE save/restore, restoration on exception, nested usage
    - test_keychain.py has 3+ test functions covering: get_api_key tries keyring first, fallback to config, save writes to both
    - All tests use unittest.mock.patch to avoid real pyautogui/keyring calls
    - pytest tests/test_clicker_engine.py tests/test_safety.py tests/test_keychain.py -x --tb=short -q passes
  </acceptance_criteria>
  <action>
    1. Create `tests/test_clicker_engine.py`:
       - Follow the existing test pattern (pytest classes with descriptive test methods).
       - Test class 1 `TestClickerEngine`:
         - `test_empty_strategies_returns_failure()`: Engine with empty strategy list returns `ClickResult(False, ...)`.
         - `test_first_successful_strategy_wins()`: Two strategies where first succeeds, second is never consulted.
         - `test_fallthrough_to_next_strategy()`: First fails, second succeeds, result reports second strategy name.
         - `test_execute_with_failsafe_preserves_state()`: mock pyautogui.FAILSAFE, verify it's restored after execute_with_failsafe even on exception.
       - Use `from unittest.mock import patch, MagicMock` for pyautogui mocking.
       - Import `from qwen_desktop.core.clickers import ClickerEngine, ClickStrategy, ClickResult`.
       - Import `from qwen_desktop.core.clickers.strategies.template import TemplateClickStrategy`.

    2. Create `tests/test_safety.py`:
       - Test class 1 `TestRestoreFailsafe`:
         - `test_saves_and_restores_state()`: patch pyautogui.FAILSAFE, enter context, verify FAILSAFE=False inside, verify original restored after.
         - `test_restores_on_exception()`: enter context, raise exception, verify FAILSAFE restored in except block.
         - `test_nested_context_works()`: two nested restore_failsafe contexts, verify each level restores correctly.
       - Import `from qwen_desktop.utils.safety import restore_failsafe`.

    3. Create `tests/test_keychain.py`:
       - Test class 1 `TestProviderConfigKeychain`:
         - `test_get_api_key_tries_keyring_first()`: mock both `keyring.get_password` (returns "keyring_key") and `settings.get("api_key")` (returns "config_key"), assert get_api_key returns "keyring_key".
         - `test_get_api_key_fallback_to_config()`: mock `keyring.get_password` (returns None/empty), settings.get returns "config_key", assert get_api_key returns "config_key".
         - `test_save_writes_to_keyring()`: mock `keyring.set_password`, call save() with api_key="new_key", verify set_password called with correct args.
       - Import `from qwen_desktop.auth.provider_config import ProviderConfig`.
       - Use `unittest.mock.patch` for both keyring and Settings.
       - Import `from qwen_desktop.config.settings import Settings`.

    4. Run `pytest tests/test_clicker_engine.py tests/test_safety.py tests/test_keychain.py -x --tb=short -q` -- all tests must pass.
  </action>
  <verify>
    <automated>Set-Location qwen-desktop; pytest tests/test_clicker_engine.py tests/test_safety.py tests/test_keychain.py -x --tb=short -q</automated>
  </verify>
  <done>
    3 new test files created with 10+ total test cases. All tests pass. Tests use mocking to avoid real pyautogui/keyring calls.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite and RFCT-05 file size compliance</name>
  <files>
    qwen-desktop/qwen_desktop/ui/assistant/controller.py (file size check)
    qwen-desktop/qwen_desktop/ui/assistant/worker.py (file size check)
    qwen-desktop/qwen_desktop/ui/assistant/message_bubble.py (file size check)
    qwen-desktop/qwen_desktop/ui/assistant/chat_popup.py (file size check)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/ui/assistant/controller.py (created in Task 1)
    @qwen-desktop/qwen_desktop/ui/assistant/worker.py (created in Task 1)
    @qwen-desktop/qwen_desktop/ui/assistant/message_bubble.py (created in Task 1)
    @qwen-desktop/qwen_desktop/ui/assistant/chat_popup.py (created in Task 1)
  </read_first>
  <acceptance_criteria>
    - pytest tests/ -x --tb=short -q passes (all 6 test files: 3 original + 3 new)
    - worker.py is under 100 lines
    - message_bubble.py is under 300 lines (it was 218 lines originally -- might grow slightly with imports)
    - chat_popup.py is under 300 lines (it was 210 lines originally)
    - controller.py is under 1000 lines (down from ~1500 in the monolithic file)
    - `ruff check qwen_desktop/ui/assistant/` passes
  </acceptance_criteria>
  <action>
    1. Run `pytest tests/ -x --tb=short -q` from qwen-desktop/ -- confirm all tests pass.
    2. Count lines in each new assistant file:
       - worker.py: target ~60 lines (was 50).
       - message_bubble.py: target ~230 lines (was 218).
       - chat_popup.py: target ~220 lines (was 210).
       - controller.py: target ~900 lines (was ~1500).
    3. Per D-03 (flexible guideline): if a file meaningfully exceeds its target, document the justification in a comment at the top of the file. For controller.py, the complexity of FloatingAssistant naturally exceeds 100 lines -- note this in a top comment.
    4. Run `ruff check qwen_desktop/ui/assistant/` and fix any lint issues.
    5. Verify `ruff check qwen_desktop/` passes or only has pre-existing non-blocking warnings.
  </action>
  <verify>
    <automated>Set-Location qwen-desktop; pytest tests/ -x --tb=short -q; if ($?) { Write-Output "Full test suite: PASSED" }</automated>
  </verify>
  <done>
    All tests pass (6 test files). Each assistant file is within reasonable size limits per D-03. Ruff lint passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI layer to core layer | FloatingAssistant imports and calls core services (API clients, executors, clicker engine) |
| config.json on disk | Settings persistence (plaintext api_key still exists as fallback per SEC-02 migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | ui/assistant/controller.py | mitigate | Click execution uses `restore_failsafe()` context manager (inherited from Plan 01-01). FAILSAFE always restored. |
| T-01-11 | Information Disclosure | ui/assistant/ tests | accept | Test files mock keyring and pyautogui. No real credentials or automation in test code. |
| T-01-12 | Elevation of Privilege | ui/assistant/controller.py -> ClickerEngine | accept | Refactoring preserves existing security posture. ClickerEngine is not adding new automation capabilities. |
| T-01-SC | Tampering | pip installs | mitigate | No new packages introduced. |
</threat_model>

<verification>
1. `python -c "from qwen_desktop.ui.assistant.controller import FloatingAssistant; from qwen_desktop.ui.assistant.worker import APIServerWorker; from qwen_desktop.ui.assistant.message_bubble import MessageBubble; from qwen_desktop.ui.assistant.chat_popup import ChatHistoryPopup"` succeeds
2. `python -c "import qwen_desktop"` succeeds
3. `pytest tests/ -x --tb=short -q` passes (6 test files: 3 original + 3 new)
4. `ruff check qwen_desktop/ui/assistant/` passes
5. Original `ui/floating_assistant.py` is deleted from filesystem
6. Each assistant file is within documented size targets (D-03 flexible guideline)
</verification>

<success_criteria>
- FloatingAssistant split into 4 focused modules under ui/assistant/
- app.py imports from new path; original 1,988-line file deleted
- All refactored modules have test coverage (test_clicker_engine.py, test_safety.py, test_keychain.py)
- Test suite passes completely
- File sizes are reasonable per D-03 flexible guideline
- Click execution logic uses ClickerEngine restore_failsafe (wired in Plans 01-01 and 01-03)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-security/01-04-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| `APIServerWorker` | QThread class | `qwen_desktop/ui/assistant/worker.py` | FloatingAssistant controller |
| `MessageBubble` | QWidget class | `qwen_desktop/ui/assistant/message_bubble.py` | ChatHistoryPopup, FloatingAssistant |
| `ChatHistoryPopup` | QWidget class | `qwen_desktop/ui/assistant/chat_popup.py` | FloatingAssistant controller |
| `FloatingAssistant` | QWidget class | `qwen_desktop/ui/assistant/controller.py` | app.py (DesktopApp) |
| `TestClickerEngine` | pytest class | `tests/test_clicker_engine.py` | CI, code review |
| `TestRestoreFailsafe` | pytest class | `tests/test_safety.py` | CI, code review |
| `TestProviderConfigKeychain` | pytest class | `tests/test_keychain.py` | CI, code review |
</output>
