---
phase: 01-foundation-security
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - qwen-desktop/qwen_desktop/core/clickers/__init__.py (new)
  - qwen-desktop/qwen_desktop/core/clickers/engine.py (new)
  - qwen-desktop/qwen_desktop/core/clickers/strategies/__init__.py (new)
  - qwen-desktop/qwen_desktop/core/clickers/strategies/template.py (new)
  - qwen-desktop/qwen_desktop/core/clickers/strategies/vision_api.py (new)
  - qwen-desktop/qwen_desktop/core/clickers/strategies/local_vision.py (new)
  - qwen-desktop/qwen_desktop/auth/provider_config.py (modify -- keychain integration)
  - qwen-desktop/qwen_desktop/config/settings.py (modify -- lazy keychain fallback)
  - qwen-desktop/qwen_desktop/core/debug_clicker.py (delete)
  - qwen-desktop/qwen_desktop/core/local_vision_clicker.py (delete)
  - qwen-desktop/qwen_desktop/core/opencv_qwen_clicker.py (delete)
  - qwen-desktop/qwen_desktop/core/opencv_qwen_clicker_enhanced.py (delete)
  - qwen-desktop/qwen_desktop/core/pixel_perfect_clicker.py (delete)
  - qwen-desktop/qwen_desktop/core/qwen_screen_clicker.py (delete)
  - qwen-desktop/qwen_desktop/core/universal_clicker.py (delete)
autonomous: true
requirements:
  - RFCT-01
  - SEC-02

must_haves:
  truths:
    - "ClickerEngine base class with pluggable ClickStrategy exists in core/clickers/"
    - "ClickerEngine inherits from perfect_clicker.py patterns per D-06"
    - "ScreenEnv/ScreenDetector are imported from utils.screen, not redefined"
    - "7 dead clicker files are deleted from the filesystem"
    - "API keys are stored in OS keychain via keyring.get_password/set_password"
    - "ProviderConfig.get_api_key() reads from keyring first, falls back to config.json"
    - "ProviderConfig.save() writes both to keyring AND config.json (lazy migration)"
  artifacts:
    - path: qwen-desktop/qwen_desktop/core/clickers/engine.py
      provides: "ClickerEngine orchestrator + ClickStrategy ABC"
      min_lines: 100
      exports: ["ClickerEngine", "ClickStrategy", "ClickResult"]
    - path: qwen-desktop/qwen_desktop/core/clickers/strategies/template.py
      provides: "Template matching click strategy"
      min_lines: 30
      exports: ["TemplateClickStrategy"]
    - path: qwen-desktop/qwen_desktop/core/clickers/strategies/vision_api.py
      provides: "VLM-based click strategy stub"
      min_lines: 20
      exports: ["VisionAPIClickStrategy"]
    - path: qwen-desktop/qwen_desktop/core/clickers/strategies/local_vision.py
      provides: "Local vision click strategy stub"
      min_lines: 20
      exports: ["LocalVisionClickStrategy"]
    - path: qwen-desktop/qwen_desktop/auth/provider_config.py
      provides: "Keychain-backed API key storage"
      contains: "keyring.get_password"
      contains: "keyring.set_password"
  key_links:
    - from: qwen-desktop/qwen_desktop/core/clickers/engine.py
      to: qwen-desktop/qwen_desktop/utils/screen.py
      via: "import ScreenEnv, ScreenDetector"
      pattern: "from qwen_desktop.utils.screen import"
    - from: qwen-desktop/qwen_desktop/auth/provider_config.py
      to: keyring library
      via: "keyring.get_password / keyring.set_password"
      pattern: "keyring\\."

---

<objective>
Create the consolidated ClickerEngine from perfect_clicker.py patterns and migrate API key storage to OS keychain.

**Purpose:** Replace 8 fragmented clicker implementations (3,284 lines of dead code) with a single ClickerEngine using pluggable strategies. Simultaneously secure API key storage by integrating the OS keychain via keyring, eliminating plaintext credential exposure in config.json.

**Output:**
- `qwen_desktop/core/clickers/` package with ClickerEngine base, ClickStrategy ABC, and 3 strategy implementations
- 7 dead clicker files deleted
- `ProviderConfig` reads/writes API keys via keyring with config.json fallback
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md

@qwen-desktop/qwen_desktop/core/perfect_clicker.py
@qwen-desktop/qwen_desktop/core/enhanced_executor.py
@qwen-desktop/qwen_desktop/auth/provider_config.py
@qwen-desktop/qwen_desktop/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClickerEngine package with pluggable strategies</name>
  <files>
    qwen-desktop/qwen_desktop/core/clickers/__init__.py (new)
    qwen-desktop/qwen_desktop/core/clickers/engine.py (new)
    qwen-desktop/qwen_desktop/core/clickers/strategies/__init__.py (new)
    qwen-desktop/qwen_desktop/core/clickers/strategies/template.py (new)
    qwen-desktop/qwen_desktop/core/clickers/strategies/vision_api.py (new)
    qwen-desktop/qwen_desktop/core/clickers/strategies/local_vision.py (new)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/core/perfect_clicker.py (full -- SelfCalibrator, coordinate conversion, click patterns)
    @qwen-desktop/qwen_desktop/core/enhanced_executor.py (ClickValidator for reusable pixel verification)
    @qwen-desktop/qwen_desktop/utils/screen.py (ScreenEnv, ScreenDetector -- from Plan 01-01)
    @qwen-desktop/qwen_desktop/utils/safety.py (restore_failsafe -- from Plan 01-01)
  </read_first>
  <acceptance_criteria>
    - `python -c "from qwen_desktop.core.clickers.engine import ClickerEngine, ClickStrategy, ClickResult"` succeeds
    - `python -c "from qwen_desktop.core.clickers.strategies.template import TemplateClickStrategy"` succeeds
    - ClickerEngine instantiable with strategy list; execute() calls strategies in order
    - All screen env imports use utils.screen
  </acceptance_criteria>
  <action>
    1. Create directory structure `qwen_desktop/core/clickers/strategies/`.
    2. `core/clickers/__init__.py`: barrel export `ClickerEngine`, `ClickStrategy`, `ClickResult` from engine.
    3. `core/clickers/engine.py`:
       - Imports: `abc.ABC, abstractmethod`, `logging`, `from dataclasses import dataclass`, `from typing import Optional`, `from qwen_desktop.utils.screen import ScreenEnv, ScreenDetector`, `from qwen_desktop.utils.safety import restore_failsafe`.
       - `@dataclass ClickResult`: `success: bool`, `x: int`, `y: int`, `strategy_used: str`, `confidence: float`.
       - `class ClickStrategy(ABC)`: abstract `click(self, x: int, y: int, **kwargs) -> ClickResult`, abstract `name -> str` property.
       - `class ClickerEngine`:
         - `__init__(self, strategies: list[ClickStrategy])` -- stores strategy list.
         - `execute(self, x, y, target_name=None) -> ClickResult` -- iterates strategies in order, returns first successful result. Returns `ClickResult(False, x, y, "none", 0.0)` if all fail.
         - `execute_with_failsafe(self, x, y, target_name=None) -> ClickResult` -- wraps execute() in `with restore_failsafe():`.
         - `detect_screen() -> ScreenEnv` -- delegates to `ScreenDetector.detect()`.
       - Module docstring and `logger = logging.getLogger(__name__)`.
    4. `core/clickers/strategies/__init__.py`: barrel export all three strategies.
    5. `core/clickers/strategies/template.py`:
       - `class TemplateClickStrategy(ClickStrategy)`: OpenCV template matching.
       - `name` returns `"template"`.
       - `click(self, x, y, template_path=None, threshold=0.7)` -- loads template, matches with cv2.matchTemplate, returns `ClickResult(True, cx, cy, "template", max_val)` on match or `ClickResult(False, x, y, "template", 0.0)` on failure.
    6. `core/clickers/strategies/vision_api.py`:
       - `class VisionAPIClickStrategy(ClickStrategy)`: passthrough stub for VLM-based clicks.
       - `name` returns `"vision_api"`; `click` returns passthrough `ClickResult(True, x, y, "vision_api", 0.0)`.
    7. `core/clickers/strategies/local_vision.py`: same pattern as vision_api.
    8. Run `ruff check qwen_desktop/core/clickers/`.
  </action>
  <verify>
    <automated>Set-Location qwen-desktop; python -c "from qwen_desktop.core.clickers import ClickerEngine; e = ClickerEngine([]); r = e.execute(100, 200); assert not r.success and r.x == 100 and r.y == 200; print('ClickerEngine OK')"</automated>
  </verify>
  <done>
    ClickerEngine + 3 strategies exist. All imports resolve. Basic instantiation and failthrough works.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate keyring into ProviderConfig for API key storage</name>
  <files>
    qwen-desktop/qwen_desktop/auth/provider_config.py (modify)
    qwen-desktop/qwen_desktop/config/settings.py (modify if needed)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/auth/provider_config.py (full file)
    @qwen-desktop/qwen_desktop/config/settings.py (full file)
    @qwen-desktop/qwen_desktop/config/defaults.py (DEFAULT_SETTINGS)
  </read_first>
  <acceptance_criteria>
    - ProviderConfig.get_api_key() tries keyring first, falls back to settings.get("api_key")
    - ProviderConfig.save() writes key to both keyring and Settings
    - No crash when keyring backend unavailable (graceful fallback)
  </acceptance_criteria>
  <action>
    1. Edit `auth/provider_config.py`:
       - Add: `import keyring` and `import logging` with `logger = logging.getLogger(__name__)`.
       - Add constants: `_KEYRING_SERVICE = "VDA-Desktop"`, `_KEYRING_ACCOUNT = "api_key"`.
       - Modify `get_api_key()`: try `keyring.get_password(_KEYRING_SERVICE, _KEYRING_ACCOUNT)` first. If returns truthy value, return it. Otherwise fallback to `self.settings.get("api_key", "")`.
       - Modify `save()`: after existing settings writes, add: `if api_key: keyring.set_password(_KEYRING_SERVICE, _KEYRING_ACCOUNT, api_key)`. Still also save to settings for backward compat.
       - Add logging in both methods for debugging.
    2. Run `ruff check qwen_desktop/auth/provider_config.py`. Fix issues.
  </action>
  <verify>
    <automated>Set-Location qwen-desktop; python -c "from qwen_desktop.auth.provider_config import ProviderConfig; from qwen_desktop.config.settings import Settings; p = ProviderConfig(Settings()); k = p.get_api_key(); print(f'get_api_key returned {repr(k[:15]) if k else \"empty/None\"}')"</automated>
  </verify>
  <done>
    ProviderConfig reads keyring first, falls back to config. save() writes to both. No crash on unavailable keyring backend.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete 7 dead clicker files after import verification</name>
  <files>
    qwen-desktop/qwen_desktop/core/debug_clicker.py (delete)
    qwen-desktop/qwen_desktop/core/local_vision_clicker.py (delete)
    qwen-desktop/qwen_desktop/core/opencv_qwen_clicker.py (delete)
    qwen-desktop/qwen_desktop/core/opencv_qwen_clicker_enhanced.py (delete)
    qwen-desktop/qwen_desktop/core/pixel_perfect_clicker.py (delete)
    qwen-desktop/qwen_desktop/core/qwen_screen_clicker.py (delete)
    qwen-desktop/qwen_desktop/core/universal_clicker.py (delete)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/core/perfect_clicker.py (confirm it remains)
  </read_first>
  <acceptance_criteria>
    - 7 files deleted; perfect_clicker.py remains
    - `python -c "import qwen_desktop"` succeeds
    - No import of deleted modules in any .py file
  </acceptance_criteria>
  <action>
    1. Verify no live code imports the files to delete: run `grep -r` for each file stem across `qwen_desktop/` -- if ANY match, investigate before deleting.
    2. Delete all 7 files.
    3. Verify `python -c "import qwen_desktop"` succeeds.
    4. Verify only perfect_clicker.py remains among clicker files.
  </action>
  <verify>
    <automated>Set-Location qwen-desktop; $files = @("debug_clicker.py","local_vision_clicker.py","opencv_qwen_clicker.py","opencv_qwen_clicker_enhanced.py","pixel_perfect_clicker.py","qwen_screen_clicker.py","universal_clicker.py"); $count = 0; foreach ($f in $files) { if (Test-Path "qwen_desktop/core/$f") { $count++ } }; if ($count -eq 0) { Write-Output "All 7 deleted OK" } else { Write-Error "$count files remain"; exit 1 }</automated>
  </verify>
  <done>
    7 dead clicker files deleted. perfect_clicker.py remains. Package imports cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application to OS keyring | API key read/write via keyring to Windows Credential Manager |
| application to config.json | API key stored plaintext as fallback during migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | auth/provider_config.py | mitigate | API keys moved from plaintext config.json to OS keychain via keyring. Lazy fallback preserves backward compatibility. |
| T-01-08 | Information Disclosure | config/settings.py fallback | accept | Plaintext api_key field kept as fallback for users with existing configs. Will be removed after all users migrate (future cleanup). |
| T-01-09 | Tampering | core/clickers/engine.py | mitigate | restore_failsafe wraps all pyautogui click operations, ensuring FAILSAFE restored on exception. |
| T-01-SC | Tampering | pip installs | mitigate | No new packages. keyring, pyautogui already in requirements.txt, verified installed. |
</threat_model>

<verification>
1. `python -c "from qwen_desktop.core.clickers import ClickerEngine, ClickStrategy, ClickResult"` succeeds
2. `python -c "from qwen_desktop.core.clickers.strategies.template import TemplateClickStrategy"` succeeds
3. `python -c "from qwen_desktop.auth.provider_config import ProviderConfig; from qwen_desktop.config.settings import Settings; ProviderConfig(Settings()).get_api_key()"` succeeds
4. `ruff check qwen_desktop/core/clickers/ qwen_desktop/auth/provider_config.py` passes
5. `python -c "import qwen_desktop"` succeeds
6. All 7 dead clicker files deleted; perfect_clicker.py remains
</verification>

<success_criteria>
- ClickerEngine with pluggable ClickStrategy ABC exists in core/clickers/ with 3 strategy implementations
- All ScreenEnv/ScreenDetector usage imports from utils.screen
- 7 dead clicker files deleted (3,284 lines of dead code removed)
- ProviderConfig reads API keys from keyring first, falls back to config.json; save() writes to both
</success_criteria>

<output>
Create `.planning/phases/01-foundation-security/01-03-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| `ClickerEngine` | class | `qwen_desktop/core/clickers/engine.py` | FloatingAssistant controller, future agent loop |
| `ClickStrategy` | ABC | `qwen_desktop/core/clickers/engine.py` | All strategy implementations |
| `ClickResult` | dataclass | `qwen_desktop/core/clickers/engine.py` | ClickerEngine.execute() return type |
| `TemplateClickStrategy` | class | `qwen_desktop/core/clickers/strategies/template.py` | ClickerEngine strategy |
| `VisionAPIClickStrategy` | class | `qwen_desktop/core/clickers/strategies/vision_api.py` | ClickerEngine strategy |
| `LocalVisionClickStrategy` | class | `qwen_desktop/core/clickers/strategies/local_vision.py` | ClickerEngine strategy |
| `_KEYRING_SERVICE` | constant | `qwen_desktop/auth/provider_config.py` | keyring service name |
| `_KEYRING_ACCOUNT` | constant | `qwen_desktop/auth/provider_config.py` | keyring account name |
</output>
