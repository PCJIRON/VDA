---
phase: 01-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qwen-desktop/qwen_desktop/utils/screen.py (new)
  - qwen-desktop/qwen_desktop/utils/safety.py (new)
  - qwen-desktop/qwen_desktop/core/enhanced_executor.py (modify)
  - qwen-desktop/qwen_desktop/ui/floating_assistant.py (modify)
  - qwen-desktop/tests/test_auth.py (delete)
  - qwen-desktop/tests/test_conversation.py (delete)
autonomous: true
requirements:
  - RFCT-04
  - SEC-03
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "ScreenEnv dataclass and ScreenDetector class exist in utils/screen.py, importable from qwen_desktop.utils.screen"
    - "restore_failsafe context manager in utils/safety.py saves pyautogui.FAILSAFE before block and restores it in finally"
    - "enhanced_executor.py has no global pyautogui.FAILSAFE = False at module level"
    - "floating_assistant.py _execute_uied_action wraps click code with restore_failsafe context manager"
    - "pytest tests/ -x -q completes without ModuleNotFoundError (broken test files deleted)"
  artifacts:
    - path: qwen-desktop/qwen_desktop/utils/screen.py
      provides: "ScreenEnv dataclass + ScreenDetector class"
      min_lines: 200
      exports: ["ScreenEnv", "ScreenDetector"]
    - path: qwen-desktop/qwen_desktop/utils/safety.py
      provides: "restore_failsafe context manager"
      min_lines: 25
      exports: ["restore_failsafe"]
    - path: qwen-desktop/qwen_desktop/core/enhanced_executor.py
      provides: "Clean FAILSAFE state — no global mutation"
      contains: "restore_failsafe"
      does_not_contain: "pyautogui.FAILSAFE = False"
    - path: qwen-desktop/qwen_desktop/ui/floating_assistant.py
      provides: "FAILSAFE-safe _execute_uied_action"
      contains: "restore_failsafe"
  key_links:
    - from: qwen-desktop/qwen_desktop/utils/screen.py
      to: qwen-desktop/qwen_desktop/core/perfect_clicker.py
      via: "import ScreenDetector (replacing inline copy)"
      pattern: "from qwen_desktop.utils.screen import"
    - from: qwen-desktop/qwen_desktop/utils/safety.py
      to: qwen-desktop/qwen_desktop/core/enhanced_executor.py
      via: "with restore_failsafe():"
      pattern: "restore_failsafe"
    - from: qwen-desktop/qwen_desktop/utils/safety.py
      to: qwen-desktop/qwen_desktop/ui/floating_assistant.py
      via: "with restore_failsafe(): wrapping pyautogui actions"
      pattern: "restore_failsafe"

---

<objective>
Establish shared foundation utilities (ScreenEnv/ScreenDetector and FAILSAFE safety) and fix the broken test suite.

**Purpose:** Eliminate duplicate screen detection code that exists identically in multiple clicker files, prevent FAILSAFE-disabled-during-exception bugs via a reusable context manager, and restore the test suite to a working state.

**Output:**
- `qwen_desktop/utils/screen.py` — ScreenEnv dataclass + ScreenDetector (shared, single source of truth)
- `qwen_desktop/utils/safety.py` — restore_failsafe context manager
- Clean FAILSAFE sites in enhanced_executor.py and floating_assistant.py
- Working test suite (broken test files deleted)
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md

Reference source files:
@qwen-desktop/qwen_desktop/core/perfect_clicker.py (ScreenEnv + ScreenDetector lines 38–260)
@qwen-desktop/qwen_desktop/core/enhanced_executor.py (FAILSAFE line 13, full file)
@qwen-desktop/qwen_desktop/ui/floating_assistant.py (FAILSAFE lines 1958, 1981)
@qwen-desktop/tests/test_auth.py (to delete)
@qwen-desktop/tests/test_conversation.py (to delete)
@qwen-desktop/tests/test_attachments.py (verify it still passes)
@qwen-desktop/tests/test_error_handler.py (verify it still passes)
@qwen-desktop/tests/test_file_encoder.py (verify it still passes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ScreenEnv and ScreenDetector to utils/screen.py</name>
  <files>
    qwen-desktop/qwen_desktop/utils/screen.py (new)
    qwen-desktop/qwen_desktop/core/perfect_clicker.py (modify — remove ScreenEnv/ScreenDetector, import from utils.screen)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/core/perfect_clicker.py (lines 38-260 — ScreenEnv dataclass + ScreenDetector + _detect_windows/_detect_mac/_detect_linux + _get_windows_monitors)
    @qwen-desktop/qwen_desktop/utils/__init__.py (existing barrel exports pattern)
  </read_first>
  <acceptance_criteria>
    - ScreenEnv dataclass and ScreenDetector class exist in qwen_desktop/utils/screen.py
    - `python -c "from qwen_desktop.utils.screen import ScreenEnv, ScreenDetector"` succeeds
    - perfect_clicker.py imports ScreenEnv/ScreenDetector from utils.screen instead of defining them inline
    - `python -c "from qwen_desktop.core.perfect_clicker import ScreenEnv"` still succeeds (re-export from new location)
  </acceptance_criteria>
  <action>
    1. Create `qwen_desktop/utils/screen.py`:
       - Copy ScreenEnv dataclass from perfect_clicker.py lines 38-52 with full field set (os, logical_w, logical_h, physical_w, physical_h, scale_x, scale_y, dpi, is_hidpi, monitors, wayland).
       - Copy ScreenDetector class including detect() static method and _detect_windows, _detect_mac, _detect_linux, _get_windows_monitors. Keep identical method bodies — this is an extraction, not a rewrite.
       - Add module-level logger: `logger = logging.getLogger(__name__)`
       - Add module docstring: `"""Screen environment detection — DPI, monitor layout, OS platform."""`
       - Preserve all imports: cv2, numpy, pyautogui, platform, subprocess, ctypes, json, time, from PIL import Image, from typing import Tuple, Optional, List, Dict, Any.
       - Follow existing conventions: Google-style docstrings on all public methods (ScreenDetector.detect, _detect_windows, _detect_mac, _detect_linux, _get_windows_monitors).
    2. Edit `qwen_desktop/core/perfect_clicker.py`:
       - Remove the ScreenEnv dataclass definition (lines 38-52) and ScreenDetector class (lines 54-260 including all methods).
       - In their place, add: `from qwen_desktop.utils.screen import ScreenEnv, ScreenDetector`
       - Adjust imports: remove `from PIL import Image` only if it becomes unused (keep if other code still uses it).
    3. Update `qwen_desktop/utils/__init__.py`:
       - Add barrel export: `from qwen_desktop.utils.screen import ScreenEnv, ScreenDetector` if the package convention requires it. Skip if __init__.py is intentionally sparse — check existing pattern.
    4. Verify: Run `python -c "from qwen_desktop.utils.screen import ScreenEnv, ScreenDetector; from qwen_desktop.core.perfect_clicker import ScreenEnv as SE; assert ScreenEnv == SE; print('OK')"` to confirm ScreenEnv identity is preserved via re-export.
  </action>
  <verify>
    <automated>python -c "from qwen_desktop.utils.screen import ScreenEnv, ScreenDetector; print('Screen env extracted OK')"</automated>
  </verify>
  <done>
    ScreenEnv dataclass and ScreenDetector class live only in utils/screen.py. perfect_clicker.py imports them. Both import paths resolve without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FAILSAFE context manager and fix enhanced_executor.py</name>
  <files>
    qwen-desktop/qwen_desktop/utils/safety.py (new)
    qwen-desktop/qwen_desktop/core/enhanced_executor.py (modify — remove global FAILSAFE mutation, wrap click code with context manager)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/utils/__init__.py (barrel export pattern)
    @qwen-desktop/qwen_desktop/core/enhanced_executor.py (full file, especially line 13 and ClickValidator.click_and_verify)
  </read_first>
  <acceptance_criteria>
    - `python -c "from qwen_desktop.utils.safety import restore_failsafe; print('OK')"` succeeds
    - enhanced_executor.py has NO `pyautogui.FAILSAFE = False` at module level
    - enhanced_executor.py ClickValidator methods use `with restore_failsafe()` where they call pyautogui click functions
    - `ruff check qwen_desktop/core/enhanced_executor.py qwen_desktop/utils/safety.py` passes
  </acceptance_criteria>
  <action>
    1. Create `qwen_desktop/utils/safety.py`:
       - Follow D-09 pattern: `@contextmanager` from `contextlib` that saves and restores `pyautogui.FAILSAFE`.
       - Implementation: save `pyautogui.FAILSAFE` to `original`, set `pyautogui.FAILSAFE = False`, `try: yield`, `finally: pyautogui.FAILSAFE = original` with a `logger.debug` on restore.
       - Add module docstring: `"""Safety utilities — FAILSAFE context manager and related automation safety helpers."""`
       - Export only `restore_failsafe` in __all__.
    2. Edit `qwen_desktop/core/enhanced_executor.py`:
       - Remove line 13: `pyautogui.FAILSAFE = False` — delete this line entirely (D-10).
       - Add import: `from qwen_desktop.utils.safety import restore_failsafe`
       - In `ClickValidator.click_and_verify` (line 68-89): wrap the `try/except` block body (the pyautogui.click call and related operations) inside `with restore_failsafe()`.
       - The click_and_verify method body after the retry loop setup (`for attempt in range(retries + 1):`) should contain `with restore_failsafe():` wrapping the click operations.
       - Ensure FAILSAFE state is properly restored even if the pyautogui operation raises an exception.
    3. Update `utils/__init__.py` if it follows barrel export pattern.
    4. Run `ruff check qwen_desktop/core/enhanced_executor.py qwen_desktop/utils/safety.py` — fix any lint errors.
  </action>
  <verify>
    <automated>python -c "from qwen_desktop.utils.safety import restore_failsafe; import pyautogui; orig = pyautogui.FAILSAFE; pyautogui.FAILSAFE = not orig; with restore_failsafe(): assert not pyautogui.FAILSAFE; assert pyautogui.FAILSAFE == (not orig); pyautogui.FAILSAFE = orig"</automated>
  </verify>
  <done>
    utils/safety.py exists with `restore_failsafe`. enhanced_executor.py has no global `FAILSAFE = False`. All pyautogui click calls are wrapped with the context manager.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix FAILSAFE in floating_assistant.py and delete broken test files</name>
  <files>
    qwen-desktop/qwen_desktop/ui/floating_assistant.py (modify — wrap FAILSAFE sites)
    qwen-desktop/tests/test_auth.py (delete)
    qwen-desktop/tests/test_conversation.py (delete)
  </files>
  <read_first>
    @qwen-desktop/qwen_desktop/ui/floating_assistant.py (lines 1942-1988 — _execute_uied_action method)
  </read_first>
  <acceptance_criteria>
    - floating_assistant.py _execute_uied_action uses `with restore_failsafe():` wrapping all pyautogui click/type operations
    - No `pyautogui.FAILSAFE = True` or `= False` remains in floating_assistant.py (0 grep hits for `FAILSAFE`)
    - test_auth.py and test_conversation.py are deleted from filesystem
    - `pytest tests/ -x --tb=short -q` passes (remaining tests: test_attachments, test_error_handler, test_file_encoder)
  </acceptance_criteria>
  <action>
    1. Edit `qwen_desktop/ui/floating_assistant.py`:
       - Add import at top: `from qwen_desktop.utils.safety import restore_failsafe`
       - In `_execute_uied_action` (line 1942-1988): replace the inline FAILSAFE pattern.
       - Line 1958: `pyautogui.FAILSAFE = False` → delete this line.
       - Lines 1960-1979: wrap the `if action == 'click':` ... `elif action == 'double_click':` ... `elif action == 'type':` blocks inside a `with restore_failsafe():` context. The structure should be: `with restore_failsafe():` followed by indented if/elif chain, followed by `logger.info(f"UIED: Action '{action}' completed successfully")` (line 1983) inside the context.
       - Line 1981: `pyautogui.FAILSAFE = True` → delete this line (the context manager's finally handles restoration).
       - Verify no bare `pyautogui.FAILSAFE` assignments remain.
    2. Delete broken test files (D-11):
       - Remove `qwen-desktop/tests/test_auth.py` (109 lines, imports non-existent `qwen_desktop.auth.oauth_handler`, `token_manager`, `credentials`).
       - Remove `qwen-desktop/tests/test_conversation.py` (185 lines, imports non-existent `qwen_desktop.core.conversation`).
    3. Run `pytest tests/ -x --tb=short -q` from the `qwen-desktop/` directory — confirm all 3 remaining test files pass.
    4. Run `grep -r "pyautogui.FAILSAFE" qwen_desktop/` — confirm exactly 0 matches remain across the entire codebase (ensuring no missed FAILSAFE sites).
  </action>
  <verify>
    <automated>Set-Location qwen-desktop; pytest tests/ -x --tb=short -q; if ($?) { $result = Select-String -Path "qwen_desktop/ui/floating_assistant.py" -Pattern "restore_failsafe" -SimpleMatch; if ($result) { Write-Output "FAILSAFE fix applied" } else { Write-Error "FAILSAFE not found" } }</automated>
  </verify>
  <done>
    floating_assistant.py uses restore_failsafe context manager. No pyautogui.FAILSAFE assignments remain anywhere in the codebase. Tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → pyautogui | Desktop automation commands cross from code to OS input simulation |
| config.json on disk | API keys stored in plaintext (migration target for later plans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | enhanced_executor.py:13 | mitigate | Remove global `pyautogui.FAILSAFE = False` (Task 2). FAILSAFE context manager ensures restore in `finally`. |
| T-01-02 | Tampering | floating_assistant.py:1958/1981 | mitigate | Replace inline FAILSAFE manipulation with `restore_failsafe()` context manager that always restores state. |
| T-01-03 | Information Disclosure | utils/screen.py | accept | ScreenEnv data (resolution, OS, monitors) is non-sensitive — no PII or secrets. |
| T-01-04 | Information Disclosure | config/settings.py (plaintext API key) | accept | Keychain migration is SEC-02 (Plan 01-03). This plan does not resolve it — explicitly deferred by phase design. |
| T-01-SC | Tampering | pip installs | mitigate | No new packages introduced. All dependencies already in requirements.txt and verified installed. |
</threat_model>

<verification>
1. `python -c "from qwen_desktop.utils.screen import ScreenEnv, ScreenDetector"` succeeds
2. `python -c "from qwen_desktop.utils.safety import restore_failsafe"` succeeds
3. `grep -r "pyautogui.FAILSAFE" qwen_desktop/` returns zero matches
4. `ruff check qwen_desktop/utils/ qwen_desktop/core/enhanced_executor.py qwen_desktop/ui/floating_assistant.py` passes (or exits with known, non-blocking warnings)
5. `cd qwen-desktop && pytest tests/ -x --tb=short -q` passes with 3 test files
</verification>

<success_criteria>
- ScreenEnv/ScreenDetector are importable from utils.screen and perfect_clicker.py imports them
- FAILSAFE context manager exists and enhanced_executor.py has no global FAILSAFE = False
- Test suite runs without import errors (broken test files deleted)
- Zero pyautogui.FAILSAFE assignments remain in the codebase
</success_criteria>

<output>
Create `.planning/phases/01-foundation-security/01-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| `ScreenEnv` | dataclass | `qwen_desktop/utils/screen.py` | ClickerEngine, perfect_clicker.py, any screen-detection consumer |
| `ScreenDetector` | class | `qwen_desktop/utils/screen.py` | ClickerEngine, perfect_clicker.py |
| `restore_failsafe` | context manager | `qwen_desktop/utils/safety.py` | enhanced_executor.py, floating_assistant.py, all pyautogui consumers |
</output>
