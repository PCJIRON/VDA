---
phase: 02-agent-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["qwen_desktop/core/tool_registry.py", "qwen_desktop/core/tool_executor/base_tool.py", "qwen_desktop/config/settings.py"]
autonomous: true
requirements: ["AGNT-01", "AGNT-06"]
user_setup: []
must_haves:
  truths:
    - "ToolRegistry can register and retrieve tool implementations by name"
    - "BaseTool provides abstract execute method and metadata"
    - "Settings expose configuration entries to enable/disable individual tools"
  artifacts:
    - path: "qwen_desktop/core/tool_registry.py"
      provides: "ToolRegistry singleton with register/get methods"
    - path: "qwen_desktop/core/tool_executor/base_tool.py"
      provides: "BaseTool abstract class with execute signature"
    - path: "qwen_desktop/config/settings.py"
      provides: "Tool config section with defaults and load/save"
  key_links:
    - from: "qwen_desktop/core/tool_registry.py"
      to: "qwen_desktop/core/tool_executor/base_tool.py"
      via: "inherits BaseTool"
    - from: "qwen_desktop/config/settings.py"
      to: "qwen_desktop/core/tool_registry.py"
      via: "registry reads enabled flags from Settings"
---

<objective>
Establish a unified tool registration system and configuration scaffolding to support future tool implementations.

Purpose: Enables modular addition of Web, Terminal, File, and Voice tools without tightly coupling code.
Output: ToolRegistry module, BaseTool abstract class, Settings extensions.
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@qwen_desktop/core/tool_executor/executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToolRegistry singleton</name>
  <files>qwen_desktop/core/tool_registry.py</files>
  <action>Implement a module-level ToolRegistry class with register(name: str, tool: BaseTool) and get(name: str) -> BaseTool methods, thread‑safe dictionary storage, and a global get_registry() accessor.</action>
  <verify><automated>python - <<PY
import importlib, sys, pathlib, json, os
module = importlib.import_module('qwen_desktop.core.tool_registry')
assert hasattr(module, 'ToolRegistry'), 'ToolRegistry missing'
registry = module.get_registry()
assert hasattr(registry, 'register') and hasattr(registry, 'get'), 'Methods missing'
print('PASS')
PY</automated></verify>
  <done>ToolRegistry can register a dummy BaseTool subclass and retrieve it without error.</done>
</task>

<task type="auto">
  <name>Task 2: Define BaseTool abstract class</name>
  <files>qwen_desktop/core/tool_executor/base_tool.py</files>
  <action>Create BaseTool with abstract async def execute(self, *args, **kwargs) -> Any and class attribute name:str. Include __repr__ for debugging.</action>
  <verify><automated>python - <<PY
from qwen_desktop.core.tool_executor.base_tool import BaseTool
import abc
assert issubclass(BaseTool, abc.ABC), 'BaseTool not abstract'
PY</automated></verify>
  <done>BaseTool class is importable and defines abstract execute.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Settings with tool config</name>
  <files>qwen_desktop/config/settings.py</files>
  <action>Add a dictionary attribute DEFAULT_TOOL_CONFIG with keys for "web", "terminal", "file", "voice" each boolean default True. Ensure Settings loads and saves these flags.</action>
  <verify><automated>python - <<PY
from qwen_desktop.config.settings import Settings
s = Settings()
assert hasattr(s, 'tool_config'), 'tool_config missing'
assert isinstance(s.tool_config, dict), 'tool_config not dict'
print('PASS')
PY</automated></verify>
  <done>Settings instance contains tool_config with expected keys and persistence works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI ⇢ Tool Registry | User actions trigger tool execution via registry; ensure only allowed tools per Settings are invoked |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-02-01 | Authorization | ToolRegistry.get | mitigate | Verify tool is enabled in Settings before returning instance |
| T-02-02 | Integrity | Settings.tool_config file | mitigate | Validate JSON schema on load, reject unexpected keys |
</threat_model>

<verification>
Run the three verification commands; all must output PASS.
</verification>

<success_criteria>
All tasks pass automated checks, code style passes ruff, and no import errors across the project.
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-01-PLAN.md` when done
</output>
