---
phase: 02-agent-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["qwen_desktop/ui/components/thinking_panel.py", "qwen_desktop/ui/assistant/controller.py"]
autonomous: true
requirements: ["AGNT-03", "GUI-06"]
user_setup: []
must_haves:
  truths:
    - "Thinking panel appears below chat input and can be expanded/collapsed"
    - "Panel displays step label, status icon, and one-line tool detail for each agent step"
    - "Doom loop detection shows red warning badge in the panel and pauses execution"
  artifacts:
    - path: "qwen_desktop/ui/components/thinking_panel.py"
      provides: "ThinkingPanel widget with animation and step UI"
    - path: "qwen_desktop/ui/assistant/controller.py"
      provides: "Integration of ThinkingPanel into FloatingAssistant and update handling"
  key_links:
    - from: "qwen_desktop/ui/components/thinking_panel.py"
      to: "qwen_desktop/core/agent_manager/agent_manager.py"
      via: "AgentManager emits step events captured by UI"
    - from: "qwen_desktop/ui/components/thinking_panel.py"
      to: "qwen_desktop/core/agent_manager/doom_detector.py"
      via: "Doom loop detection triggers UI badge via signal"
---

<objective>
Implement the thinking visualization panel per decisions D-01 to D-04, providing UI feedback for agent steps and doom loop detection.

Purpose: Gives users transparent view of the agent's reasoning process and alerts on doom loops.
Output: ThinkingPanel widget, UI integration, signal connections.
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@qwen_desktop/ui/assistant/controller.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThinkingPanel widget</name>
  <files>qwen_desktop/ui/components/thinking_panel.py</files>
  <action>Implement a QWidget subclass with a QVBoxLayout containing a header label, collapsible content area, and status icon. Use QPropertyAnimation to animate height on expand/collapse. Provide methods add_step(step_label: str, detail: str) and update_step_status(index: int, status: str) where status maps to icons (spinner, checkmark, X). Include a red badge area for doom loop warnings.</action>
  <verify><automated>python - <<PY
from qwen_desktop.ui.components.thinking_panel import ThinkingPanel
p = ThinkingPanel()
assert hasattr(p, 'add_step') and hasattr(p, 'update_step_status'), 'Methods missing'
print('PASS')
PY</automated></verify>
  <done>ThinkingPanel class instantiates, methods exist, and animation properties are set.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ThinkingPanel into FloatingAssistant</name>
  <files>qwen_desktop/ui/assistant/controller.py</files>
  <action>Import ThinkingPanel, instantiate in FloatingAssistant UI below the message input, and connect AgentManager step signals to panel methods. Ensure panel respects D-01 (expandable below chat) and D-02 (medium detail). Add signal-slot for doom loop detection to display red badge per D-04.</action>
  <verify><automated>python - <<PY
from qwen_desktop.ui.assistant.controller import FloatingAssistant
fa = FloatingAssistant()
assert hasattr(fa, 'thinking_panel'), 'thinking_panel not attached'
print('PASS')
PY</automated></verify>
  <done>FloatingAssistant includes a collapsible ThinkingPanel that updates on agent steps.</done>
</task>

<task type="auto">
  <name>Task 3: Wire doom loop badge updates</name>
  <files>qwen_desktop/ui/components/thinking_panel.py</files>
  <action>Add a slot on_doom_loop_detected(bool) that toggles a red warning badge on the panel. Connect DoomLoopDetector signals from AgentManager to this slot.</action>
  <verify><automated>python - <<PY
from qwen_desktop.ui.components.thinking_panel import ThinkingPanel
p = ThinkingPanel()
# simulate signal call
p.on_doom_loop_detected(True)
assert p.doom_badge.isVisible(), 'Badge not visible'
print('PASS')
PY</automated></verify>
  <done>Doom loop detection displays red badge and pauses UI state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AgentManager → UI | Agent step events drive UI; ensure only allowed UI updates per Settings |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-02-05 | Integrity | ThinkingPanel widget state | mitigate | Validate step data before rendering; sanitize strings |
| T-02-06 | Information Disclosure | UI may expose internal tool details | mitigate | Limit detail depth per D-02 (medium detail) |
</threat_model>

<verification>
Execute all three verification commands; each must output PASS.
</verification>

<success_criteria>
All tasks pass automated checks, UI launches without errors, and panel behaves as described.
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-02-PLAN.md` when done
</output>