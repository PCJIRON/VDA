---
phase: 02-agent-core
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified: ["qwen_desktop/core/agent_manager/agent_manager.py", "qwen_desktop/core/agent_manager/doom_detector.py", "qwen_desktop/core/memory_manager.py", "tests/agent_manager/test_agent_loop.py"]
autonomous: true
requirements: ["AGNT-01", "AGNT-02", "AGNT-05"]
user_setup: []
must_haves:
  truths:
    - "AgentManager transitions correctly through all states and respects max iteration limit"
    - "DoomLoopDetector signals AgentManager, which pauses and displays warning via ThinkingPanel (already integrated)"
    - "SessionCompactor invokes MemoryManager to truncate/summary conversation when token threshold exceeded"
  artifacts:
    - path: "qwen_desktop/core/agent_manager/agent_manager.py"
      provides: "Complete state machine with proper handling of DOOM loop pause and iteration limits"
    - path: "qwen_desktop/core/memory_manager.py"
      provides: "SessionCompactor integration hooks (compact_if_needed)"
    - path: "tests/agent_manager/test_agent_loop.py"
      provides: "Tests covering full loop, doom detection pause, and compaction trigger"
  key_links:
    - from: "qwen_desktop/core/agent_manager/doom_detector.py"
      to: "qwen_desktop/core/agent_manager/agent_manager.py"
      via: "AgentManager checks detector.check_loop() after each tool execution"
    - from: "qwen_desktop/core/memory_manager.py"
      to: "qwen_desktop/core/agent_manager/agent_manager.py"
      via: "AgentManager calls MemoryManager.compact_if_needed() during ITERATE"
---

<objective>
Finalize the core agent loop, ensuring robust doom‑loop handling and automatic session compaction.

Purpose: Satisfies AGNT‑01 (loop), AGNT‑02 (doom detection), and AGNT‑05 (session compaction) requirements for Phase 2.
Output: Verified AgentManager state machine, integrated compaction hook, and passing unit tests.
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@qwen_desktop/core/agent_manager/agent_manager.py
@qwen_desktop/core/agent_manager/doom_detector.py
@qwen_desktop/core/memory_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden AgentManager state transitions</name>
  <files>qwen_desktop/core/agent_manager/agent_manager.py</files>
  <action>Review and update _handle_execute, _handle_verify, and iteration logic to:
- Call DoomLoopDetector.check_loop() after each tool result; if loop detected, emit a "doom_loop" event that will be caught by UI (ThinkingPanel) and set state PAUSED.
- Enforce max_iterations (default 10) – when exceeded, transition to ERROR with clear message.
- Add logging for each state change and loop detection.
</action>
  <verify><automated>python - <<PY
from qwen_desktop.core.agent_manager.agent_manager import AgentManager, AgentState
assert hasattr(AgentManager, '_handle_execute'), 'Missing handler'
print('PASS')
PY</automated></verify>
  <done>AgentManager includes loop detection checks and respects iteration limits.</done>
</task>

<task type="auto">
  <name>Task 2: Connect DoomLoopDetector to UI via ThinkingPanel</name>
  <files>qwen_desktop/core/agent_manager/doom_detector.py, qwen_desktop/ui/components/thinking_panel.py</files>
  <action>Ensure DoomLoopDetector.check_loop() result is forwarded by AgentWorker (see signal doom_loop_detected) and that ThinkingPanel.on_doom_loop_detected slot (implemented in plan 02) receives the boolean to show red badge.
</action>
  <verify><automated>python - <<PY
from qwen_desktop.core.agent_manager.doom_detector import DoomLoopDetector
assert DoomLoopDetector().check_loop() is None
print('PASS')
PY</automated></verify>
  <done>Doom loop detection updates UI badge without error.</done>
</task>

<task type="auto">
  <name>Task 3: Add session compaction hook</name>
  <files>qwen_desktop/core/memory_manager.py</files>
  <action>Implement a method compact_if_needed(current_tokens: int, max_tokens: int) that triggers self.compact() when current_tokens > max_tokens. AgentManager will call this during ITERATE state.
</action>
  <verify><automated>python - <<PY
from qwen_desktop.core.memory_manager import MemoryManager
mm = MemoryManager()
assert hasattr(mm, 'compact_if_needed'), 'Method missing'
print('PASS')
PY</automated></verify>
  <done>MemoryManager can compact session history on demand.</done>
</task>

<task type="auto">
  <name>Task 4: Unit test full agent loop with doom & compaction</name>
  <files>tests/agent_manager/test_agent_loop.py</files>
  <action>Write pytest that:
- Mocks a tool that returns identical result three times to trigger doom detection.
- Sets a low token threshold to force compaction.
- Runs AgentWorker with a simple multi‑step plan and asserts final state is COMPLETE, doom badge flag was set, and MemoryManager.compact_if_needed was called.
</action>
  <verify><automated>pytest -q tests/agent_manager/test_agent_loop.py::test_full_loop</automated></verify>
  <done>Test passes, confirming integrated loop, doom handling, and compaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AgentManager → ToolExecutor | Must verify tool permissions per PermissionSystem (AGNT‑04) before execution.
| AgentManager → MemoryManager | Compaction must not truncate essential context; enforce size limits.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-02-09 | Authorization | Tool execution via AgentManager | mitigate | PermissionSystem.check_permission called before each tool call.
| T-02-10 | Integrity | Compaction logic | mitigate | Validate that compacted data remains parseable JSON.
</threat_model>

<verification>
All verification commands must output PASS and the pytest suite must succeed.
</verification>

<success_criteria>
Agent loop operates end‑to‑end, doom detection pauses execution with UI feedback, session compaction triggers correctly, and no lint/test failures.
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-04-PLAN.md` when done
</output>
