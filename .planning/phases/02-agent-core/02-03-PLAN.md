---
phase: 02-agent-core
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: ["qwen_desktop/core/agent_manager/sub_agent_delegator.py", "qwen_desktop/core/agent_manager/agent_manager.py", "tests/agent_manager/test_sub_agent_delegation.py"]
autonomous: true
requirements: ["AGNT-03"]
user_setup: []
must_haves:
  truths:
    - "AgentManager can delegate sub-tasks to SubAgentDelegator and receive results"
    - "SubAgentDelegator spawns a new AgentWorker with its own tool set and returns its final result"
    - "Delegation failures are reported back to AgentManager and trigger ITERATE state"
  artifacts:
    - path: "qwen_desktop/core/agent_manager/sub_agent_delegator.py"
      provides: "SubAgentDelegator class with delegate(task_description) method"
    - path: "qwen_desktop/core/agent_manager/agent_manager.py"
      provides: "AgentManager method _handle_sub_agent_delegation calling delegator"
    - path: "tests/agent_manager/test_sub_agent_delegation.py"
      provides: "Unit test verifying delegation flow and result handling"
  key_links:
    - from: "qwen_desktop/core/agent_manager/sub_agent_delegator.py"
      to: "qwen_desktop/core/agent_manager/agent_manager.py"
      via: "AgentManager imports and uses SubAgentDelegator"
---

<objective>
Add sub‑agent delegation capability to the agent loop, enabling complex tasks to be broken into specialized sub‑agents.

Purpose: Satisfies requirement AGNT-03 and prepares infrastructure for future tool‑specific sub‑agents.
Output: SubAgentDelegator implementation, AgentManager integration, and passing unit test.
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@qwen_desktop/core/agent_manager/agent_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SubAgentDelegator</name>
  <files>qwen_desktop/core/agent_manager/sub_agent_delegator.py</files>
  <action>Create SubAgentDelegator class with method delegate(task_desc: str) that:
- Instantiates a new AgentWorker with a fresh AgentManager, passing task_desc as user_input.
- Starts the worker thread and awaits its finished signal (using asyncio.Event).
- Returns the final result or raises if worker ends in error. Include proper exception handling and logging.
</action>
  <verify><automated>python - <<PY
from qwen_desktop.core.agent_manager.sub_agent_delegator import SubAgentDelegator
assert hasattr(SubAgentDelegator, 'delegate'), 'delegate method missing'
print('PASS')
PY</automated></verify>
  <done>SubAgentDelegator can be instantiated and its delegate method returns a result without error on a dummy task.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SubAgentDelegator into AgentManager</name>
  <files>qwen_desktop/core/agent_manager/agent_manager.py</files>
  <action>Import SubAgentDelegator, add a private _sub_delegator attribute in __init__, and modify the state machine to handle a new event "sub_agent_needed" (e.g., when a plan step contains "delegate": true). In that case, call self._sub_delegator.delegate(step_description) and store the result in self.results, then transition to VERIFY.
</action>
  <verify><automated>python - <<PY
from qwen_desktop.core.agent_manager.agent_manager import AgentManager
assert hasattr(AgentManager, '_sub_delegator'), 'Sub delegator not attached'
print('PASS')
PY</automated></verify>
  <done>AgentManager integrates SubAgentDelegator and can process a delegation step (simulated).</done>
</task>

<task type="auto">
  <name>Task 3: Add unit test for delegation flow</name>
  <files>tests/agent_manager/test_sub_agent_delegation.py</files>
  <action>Write pytest that mocks a simple sub‑task (e.g., "echo hello" tool) and asserts AgentManager receives the delegated result and proceeds to COMPLETE state.
</action>
  <verify><automated>pytest -q tests/agent_manager/test_sub_agent_delegation.py::test_delegation_flow</automated></verify>
  <done>Test passes, confirming end‑to‑end delegation integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AgentManager → SubAgentDelegator | Delegated sub‑agent runs its own tool calls; must respect permission system per AGNT-04 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-02-07 | Authorization | SubAgentDelegator.spawn | mitigate | Propagate Settings to sub‑agent so its PermissionSystem enforces same rules |
| T-02-08 | Integrity | Result handling | mitigate | Validate sub‑agent result schema before consumption |
</threat_model>

<verification>
All three verification commands must output PASS and pytest must succeed.
</verification>

<success_criteria>
Sub‑agent delegation works, tests pass, and no new lint failures.
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-03-PLAN.md` when done
</output>
