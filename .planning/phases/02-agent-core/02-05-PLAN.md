---
phase: 02-agent-core
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["qwen_desktop/core/agent_manager/agent_manager.py", "tests/agent_manager/test_permission_enforcement.py"]
autonomous: true
requirements: ["AGNT-04"]
user_setup: []
must_haves:
  truths:
    - "AgentManager checks PermissionSystem before executing any tool and respects ALLOW/DENY/ASK decisions"
    - "When PermissionDecision is ASK, AgentManager pauses and emits permission_required signal for UI"
    - "When PermissionDecision is DENY, tool execution is skipped and error logged"
  artifacts:
    - path: "qwen_desktop/core/agent_manager/agent_manager.py"
      provides: "Permission check integration in _handle_execute"
    - path: "tests/agent_manager/test_permission_enforcement.py"
      provides: "Test verifying permission decisions affect tool execution flow"
  key_links:
    - from: "qwen_desktop/core/agent_manager/agent_manager.py"
      to: "qwen_desktop/core/agent_manager/permission_system.py"
      via: "agent_manager calls permission_system.check_permission"
---

<objective>
Integrate PermissionSystem enforcement into the agent loop, handling ALLOW, DENY, and ASK outcomes per requirement AGNT-04.

Purpose: Ensures safe tool usage with configurable permissions, enhancing security and user control.
Output: Updated AgentManager, unit test confirming behavior.
</objective>

<execution_context>
@C:/Users/uscha/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/uscha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@qwen_desktop/core/agent_manager/agent_manager.py
@qwen_desktop/core/agent_manager/permission_system.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add permission check to execution handler</name>
  <files>qwen_desktop/core/agent_manager/agent_manager.py</files>
  <action>In _handle_execute (or appropriate execution step), retrieve the tool name and args, call self.permission_system.check_permission(tool_name, agent_type="main", args=args). Based on result:
- ALLOW: proceed with tool execution as before.
- DENY: log warning, emit step_failed with message, skip tool, transition to ITERATE.
- ASK: set AgentState.PAUSED, emit permission_required signal via AgentWorker, store pending step info to resume after user response.
Update state transitions accordingly.</action>
  <verify><automated>python - <<PY
from qwen_desktop.core.agent_manager.agent_manager import AgentManager
assert hasattr(AgentManager, '_handle_execute'), 'execute handler missing'
print('PASS')
PY</automated></verify>
  <done>AgentManager includes permission gating logic with appropriate state handling.</done>
</task>

<task type="auto">
  <name>Task 2: Write unit test for permission enforcement</name>
  <files>tests/agent_manager/test_permission_enforcement.py</files>
  <action>Create pytest that:
- Mocks a simple tool (e.g., dummy_tool) registered in ToolRegistry.
- Configures PermissionSystem to DENY this tool.
- Runs AgentManager step execution and verifies the step results in PAUSED or ITERATE with appropriate error, and that permission_required signal is emitted for ASK case.
- Also test ALLOW path proceeds normally.
</action>
  <verify><automated>pytest -q tests/agent_manager/test_permission_enforcement.py::test_permission_denied</automated></verify>
  <done>Test passes, confirming permission decisions affect execution flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AgentManager → PermissionSystem | Must enforce user-defined tool permission policies before any tool call.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-02-11 | Authorization | Permission check in AgentManager | mitigate | Ensure PermissionSystem consulted for every tool execution.
| T-02-12 | Integrity | Permission decision handling | mitigate | Validate decision values and prevent bypass.
</threat_model>

<verification>
All verification commands must output PASS and pytest must succeed.
</verification>

<success_criteria>
Permission enforcement integrated without breaking existing tool execution, tests pass, and no new lint errors.
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-05-PLAN.md` when done
</output>
